# Ttldr sample tmux binding.
# install.sh writes this portable managed block to ~/.tmux.conf.
# Press Ctrl-g without the tmux prefix.

# BEGIN Ttldr
bind-key -n C-g run-shell -b "$HOME/.local/bin/ttldr '#{pane_id}' '#{client_name}'"
# END Ttldr
